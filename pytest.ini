# pytest.ini - Root configuration for pytest
# This file ensures markers are registered when running tests from the project root

[pytest]
testpaths = tests
pythonpath = orchestrator
addopts = -v --tb=short
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests requiring Docker
